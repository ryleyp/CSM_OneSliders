# ea-slide-builder pinned dependencies (all run fully offline / local-only)
# These versions support Python 3.9+ (newer pins require Python 3.10+).
streamlit==1.50.0
pandas==2.2.3
python-pptx==1.0.2
openpyxl==3.1.5
pytesseract==0.3.13
Pillow==11.3.0
